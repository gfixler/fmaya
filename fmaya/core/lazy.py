lazy = lambda xs: (x for x in xs)
strict = list

# cmap = lambda f: lambda xs: (f(x) for x in xs)
# czip = lambda xs: lambda ys: (x, y) for (x, y) in zip (xs, ys))
# doTimes = lambda n: lambda f: (f() for _ in xrange(n))

